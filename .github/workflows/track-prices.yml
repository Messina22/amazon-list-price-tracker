name: Track prices

on:
  schedule:
    # Every day at 09:23 UTC (an off-peak, non-round minute to avoid the
    # GitHub Actions top-of-hour cron rush).
    - cron: "23 9 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: track-prices
  cancel-in-progress: false

jobs:
  track:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # No dependencies to install: tracker.py is standard library only and
      # runs on the Python already present on the runner.
      - name: Record today's prices
        run: python3 tracker.py run

      - name: Commit updated price history
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "No price changes to commit"
          else
            git commit -m "Record prices for $(date -u +%F)"
            git push
          fi
